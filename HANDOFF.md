# トレーニング記録アプリ 引き継ぎ

## 0. 最初に読むべきこと

**この引き継ぎが発生した理由は、種目の動作図（アニメーション）の品質が要求水準に届かなかったためです。** 他の機能は動いていますが、図解については作り直しを前提にしてください。詳細は §5。

成果物は `training-log.html` 一枚のみ。ビルド工程なし、依存パッケージなし、外部リクエストなし。そのままブラウザで開けば動きます。

---

## 1. 何を作るものか

自宅トレーニングの記録アプリ。利用者は1人（本人のみ）。スマホを主、PCを従とする。

### 実装済みの機能

| 機能 | 状態 |
|---|---|
| A/B/C/Dの4分割ローテーション（前回の続きを自動判定） | 動作する |
| セット記録（重量・回数/秒・きつさ1〜10） | 動作する |
| きつさに応じた次セット回数の自動調整 | 動作する |
| 休憩タイマー（音・振動・端末通知・画面スリープ抑止） | 動作する |
| 種目解説18種（手順・準備・呼吸・テンポ・可動域・間違い・代替） | 動作する |
| 部位図（前面／背面、直近N日の刺激量を濃淡表示） | 動作する |
| 履歴・種目別推移・CSV書き出し | 動作する |
| 動作アニメーション | **要求水準に未達。作り直し対象** |

---

## 2. 利用者の前提（メニュー設計の根拠。変更する場合はここを確認すること）

- 23歳、175.8cm / 62kg、BMI 20.1
- 目的は筋肥大（体を大きくする）。期限なし。判断基準は見た目と触った感じ
- 器具は **5kgダンベル×2（合計10kg）のみ**。台は **高さ30cmのベッド**
- 毎日30〜45分確保可能。時間帯は不定。睡眠6時間
- 週2回、大学まで片道50分歩く。暑くなければ月1〜3回、20〜30kmの散歩
- 食事は1日2回、自炊なし。週1回ささみを足す程度なら可能
- トレーニング歴は2週間（腹筋とダンベル上げを20分）。それ以前はなし
- 腹回りの種目は続けたい。苦手な動作なし。飽きやすいので変化が欲しい
- 痛み・既往・通院なし
- 12〜1月は忙しくなる可能性

### 設計上の帰結

- **10kgしかないため、重量での漸進ができない。** 代替手段は3つ：可動域を広げる／下ろす動作を3〜4秒にする／片側ずつ行う。UIの文言もこれに沿っている
- 限界の1〜2回手前（きつさ8〜9）まで行かないと軽量では刺激にならない。回数調整のロジックはこの前提
- 背中の種目がワンハンドロウ1つしかない。器具の制約が直撃している箇所。懸垂バーの追加を提案済みだが未購入
- 食事と睡眠がボトルネックであることは本人に伝達済み

---

## 3. ファイル構成（`training-log.html` / 約2,175行）

単一ファイル。`<style>` が約13KB、`<script>` が約78KB。

### 3.1 データ定義（303〜845行）

| 行 | 名前 | 内容 |
|---|---|---|
| 303 | `MUSCLES` | 部位15種のID→日本語名 |
| 315 | `EX` | 種目18件。`{id, name, kind, w, r, sets, p[], s[], how[], ng[], up[], base?}` |
| 538 | `ROUTINES` | A/B/C/Dの4分割。`items[] = {ex, sets, r, side?, label?, note?}` |
| 579 | `DIA` | 動作図の姿勢データ（**作り直し対象**） |
| 673 | `DETAIL` | 種目ごとの詳細解説9項目 |
| 846 | `JOINTS` | 関節16点の名前リスト |

`kind` は `"w"`（重量×回数）／`"b"`（自重×回数）／`"t"`（秒）。

`base` を持つ種目（`rdl1` = 片脚RDL）は、図・解説・手順を親から継承する。`baseOf()` `exHow()` `exNg()` `exUp()` で解決。

### 3.2 図解の描画系（846〜1048行）— 作り直し対象

```
capsule(a, b, ra, rb)      2つの円をつないだ形を多角形近似。手足1本ぶん
bodyPaths(p)               姿勢→{trunk, arms, far, head} のパス群
bodyMarkup(p, flat)        近い側・奥側・腕の縁取りを分けて組み立てる
figSVG(pose, d, wrong, ghosts)  SVG文字列を返す
toPolar(p) / parentOf(p,k) 関節座標→親子関係と角度・長さ
rigidify(a, b)             終了姿勢の骨の長さを開始姿勢に合わせる
lerpPose(a, b, t)          角度で補間（長さを保つ）
framePose(d, t)            補間後に物理拘束を解いた最終姿勢
```

### 3.3 物理拘束（1391行 `PHYS` と周辺）

```
PHYS[種目ID] = { pins: [接地している関節名], hold: "接地の説明文" }
FLOOR_Y = 139               これより下に関節は行けない
boneLens(p)                 骨の長さテーブル
solvePose(pos, lens, pins, iters)  緩和法。90回反復
```

起動時に全種目の開始・終了姿勢を `solvePose` で解き直している。アニメーション中も毎フレーム `framePose` 経由で解いている。

### 3.4 保存層（1050〜1160行）

```
state = { sessions: {日付: セッション}, program: [] }
セッション = { date, routine: "A"|"B"|"C"|"D", entries: [{ex, sets:[{w,r,rpe}]}], note }
```

- 保存先は Artifact の `db` capability。`claude.use("db")` で取得、`sessions/<YYYY-MM-DD>` に1日1ドキュメント
- `db` が `null` のときは localStorage（キー `trainlog.v1`）にフォールバック
- `onSnapshot` で購読、書き込みは `persistSession(date)` を通す

**Claude Code で別環境に移す場合、`claude.use()` は存在しません。** 保存層の差し替えが必要（§6）。

### 3.5 回数調整ロジック（1570行付近）

```
adjust(kind, r, rpe)  きつさ5以下 → +2段階 / 6 → +1段階 / 7〜9 → 据置 / 10 → −1段階
                      1段階 = 回数なら2回、秒なら5秒
suggestNext(item, e, last)
  同一セッション内に記録あり → 直前セットのきつさで調整
  なければ前回セッションの初セット × その日の平均きつさで調整
  どちらもなければ ROUTINES の既定値
clampR()  回数4〜40、秒10〜180 で頭打ち
```

### 3.6 UI（1467行以降）

`render()` が `tab` を見て `viewToday()` `viewEx()` `viewBody()` `viewHist()` のいずれかを `#view` に流し込む全置換方式。`wire()` でイベントを張り直す。仮想DOMなし。

レスポンシブは `@media (min-width:900px)` で切り替え。PCではナビが左の縦並びになり、種目タブと「からだ」タブが `.splitcols` で2カラムになる。

---

## 4. 動作環境の制約（Artifact として公開する場合）

Claude Code 側で自前ホスティングに移すなら以下は不要になるが、Artifact のまま続けるなら守る必要がある。

- **外部スクリプトは4ホストのみ許可**：cdnjs.cloudflare.com / cdn.jsdelivr.net/npm/ / cdn.tailwindcss.com / code.jquery.com。unpkg・esm.sh は不可。ES Modules 不可、UMDビルドを `<script>` で読む
- 外部画像・外部API不可。CSSとJSはインライン。ファイル16MB上限
- フォントは fonts.googleapis.com のみ。現状 Archivo（数字用）を読み、日本語は端末のゴシックにフォールバック
- 保存は `db` capability、ファイル書き出しは `downloads` capability
- **メールは送れない。** 当初「休憩終了をGmailで通知」という要望があったが、(1)90秒の休憩に対してメールの配信遅延が大きすぎる (2)ページを開いている間しか動けないので閉じていれば送れず、開いていれば画面のタイマーで足りる (3)1回のトレーニングで15〜20通になる、という理由で音・振動・端末通知に置き換えた。判断は本人に説明済み
- `Notification` API と Wake Lock は iframe 内で塞がれる可能性がある。try/catch で握りつぶし、設定欄に可否を表示している。**実機での動作は未確認**

---

## 5. 未達の部分：動作アニメーション

### 経緯

4回作り直したが解決しなかった。

1. 棒人間＋2キーフレームの直線補間 → 動作中に手足が伸び縮みした
2. 太さを持つシルエット＋関節角度での補間 → 開始・終了で骨の長さが最大44%食い違っていたのが露呈。自動で揃える処理を追加
3. 接地点が1か所しか固定されず体が宙に浮いた → 接地点を複数固定する拘束ソルバを追加
4. 数値検査は全項目クリアしたが、**見た目として要求水準に達していない**

### 数値上の現状

18種目 × 動作中11地点 = 198フレームを検査して違反0：

- 骨の伸縮（5%超）：0
- 接地点のずれ：0
- 床の貫通：0
- 重心が支持面の外：0

つまり**問題は物理的整合性ではなく、表現力そのもの**にある。

### 根本的な限界（作り直す際の判断材料）

現行方式は以下の構造的制約を持つ。個別の座標修正では超えられない。

- **2キーフレームしかない。** 実際の動作には中間の経路がある。例えばスクワットは膝→股関節の順に動き始めるが、2点補間では表現できない
- **正射影の2Dシルエット。** 奥行きがなく、回旋（ワンハンドロウの肩甲骨、デッドバグの体幹）が原理的に描けない
- **関節が16点しかない。** 脊柱が1本の直線なので「背中が丸まる」を描けない。これは最も重要なフォームの誤りの1つ
- **足の関節が足首1つ。** 母趾球での回転（カーフレイズ、踵の浮き）が近似でしかない
- **姿勢の座標を手で置いている。** 種目を増やすたびに人力で、しかも解剖学的に正しく置く必要がある。スケールしない

### 推奨する方向（優先順）

**案A：3Dに移行する。** three.js + リグ付き人体モデル（glTF）。関節を角度で回し、接地はIKで解く。奥行き・回旋・視点変更が得られ、1つのモデルで全種目をまかなえる。モデルの調達とライセンスが課題。Artifactのまま行くなら three.js は cdnjs のUMDで読める。

**案B：2Dのまま作り直す。** キーフレームを3〜5点に増やし、脊柱を3〜4節に分割し、足を母趾球・踵の2接点にする。姿勢データは手置きをやめ、関節角度で定義してFKで座標を出す方式にする（角度なら解剖学的な可動域の上下限で検証できる）。現行より確実に良くなるが、案Aの表現力には届かない。

**案C：自前描画をやめる。** ライセンスの取れる種目イラスト・動画を使う。品質は最も確実。費用と、18種目ぶんの調達が課題。

現行コードで**流用価値があるのは `solvePose` の拘束ソルバ**（接地点＋骨長＋床の同時解決）で、これは案A・案Bどちらでも使える。`capsule()` による描画と `DIA` の座標データは案Bでも作り直しになる。

### 検証用の道具（再現方法）

品質判定を目視だけに頼らないために、以下を組んでいた。Claude Code 側でも再構築を推奨する。

```bash
# HTMLからJSを抜き出してNodeで実行する
python3 -c "import re;s=open('training-log.html',encoding='utf-8').read();\
open('app.js','w',encoding='utf-8').write(re.findall(r'<script>(.*?)</script>',s,re.S)[0])"

# DOM等のスタブ（head.js）を前置きして結合し実行
cat head.js app.js check.js > run.js && node run.js
```

- **物理チェッカー**：全種目×11フレームで、骨長・接地点・床貫通・重心を検査
- **コンタクトシート**：`cairosvg` で全姿勢をPNG化しタイル状に並べて目視確認。`pip install cairosvg --break-system-packages`

この2つがないと、直したつもりで別の種目を壊す。

---

## 6. Claude Code へ移す場合の作業順

1. `training-log.html` を受け取り、そのままブラウザで開いて現状を確認する
2. 保存層を差し替える。`claude.use("db")` は Artifact 専用。localStorage フォールバックは既にあるので、単独で動かすだけなら `initStore()` の `db` 取得を外せば動く
3. 単一ファイルを分割する。`data/`（EX・ROUTINES・DETAIL・PHYS）、`figure/`（描画と補間）、`ui/`、`store/` の4つが自然な境界
4. 図解を §5 のいずれかの方針で作り直す。**この際、`DETAIL` の解説文と `PHYS` の接地定義は内容として有効なので流用すること**
5. 検証スクリプトを先に用意してから着手する

### 触ってはいけない箇所

- `DETAIL`（種目解説）と `EX` の `how` / `ng` / `up` — 内容は本人と確認済み
- `ROUTINES` の4分割構成 — 器具と生活条件から決めたもの。§2の前提が変わらない限り維持
- `adjust()` の閾値 — きつさ8〜9を狙う設計。軽量器具での筋肥大という目的に直結している

### 既知の未確認事項

- `Notification` API と Wake Lock が実機で動くか
- ブルガリアンスクワットとヒップスラストが**高さ30cmのベッド**で成立するか（図の想定と実際が食い違っている可能性）
- カーフレイズに使える段差が自宅にあるか
- 利用者はまだ1回も新メニューを実施していない。回数設定は実績ではなく推定値
